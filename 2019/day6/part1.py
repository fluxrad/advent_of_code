#!/usr/bin/env python3

class AdventOject:
    def __init__(name):
        self.orbits = 0
        self.name = name

    def set_orbit(obj):
        self.orbits = obj

    def orbit_count:
        if self.orbits == 0
        return orbit 

with open('input', 'r') as f:
    orbits  = f.read().splitlines()
